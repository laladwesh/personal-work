# Backend API base URL used by the frontend
# Copy this to a file named `.env` (or `.env.local`) and adjust the value per environment.
# For Create React App, environment variables must start with REACT_APP_.
REACT_APP_API_URL=http://localhost:4000
